import React from "react";

import CardOverview from "../../components/card-overview/card-overview.component";

const Game = (props) => (
  <React.Fragment>
    <CardOverview {...props} />
  </React.Fragment>
);

export default Game;
